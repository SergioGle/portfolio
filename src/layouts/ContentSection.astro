---
export interface Props {
  id: string;
  title: string;
}

const { title, id } = Astro.props;
---

<section id={id}>
  <h2>{title}</h2>
  <p>
    <slot name="lead" />
  </p>
</section>

<style>
  section {
    scroll-margin-block-start: 9rem;
  }
</style>
